spring:
  main:
    web-application-type: none

flowable:
  external:
    worker:
      rest:
        authentication:
          bearer:
            token: ${FLOWABLE_TOKEN}

servicios:
  endpoint-url: http://localhost:8080/api/v1